"""
Fase 3 stub — Rutas FastAPI para la App Móvil.
Implementará: endpoints REST para que la app móvil consulte, revise,
modifique y apruebe CRTs y MICs directamente desde terreno.

TODO Fase 3:
- GET /api/crts — listado con filtros
- GET /api/crts/{id} — detalle + PDF generado
- PATCH /api/crts/{id} — modificación de contingencias
- POST /api/crts/{id}/approve — aprobación en terreno
- Autenticación JWT
"""
